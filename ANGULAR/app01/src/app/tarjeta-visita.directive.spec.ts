import { TarjetaVisitaDirective } from './tarjeta-visita.directive';

describe('TarjetaVisitaDirective', () => {
  it('should create an instance', () => {
    const directive = new TarjetaVisitaDirective();
    expect(directive).toBeTruthy();
  });
});
